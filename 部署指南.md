# GitHub Pages 部署指南

## 已为你准备好的文件

```
siele-workbench-deploy/
├── index.html                    ← 完整应用 (1.2MB)
├── .github/workflows/deploy.yml  ← GitHub Actions 自动部署
├── README.md
└── .gitignore
```

Git 仓库已初始化，代码已提交到 `main` 分支。

---

## 部署步骤 (3分钟搞定)

### 第1步：在 GitHub 创建新仓库

1. 打开 https://github.com/new
2. Repository name 填: `siele-workbench`（或你喜欢的名字）
3. 选择 **Public**（必须公开才能免费使用 GitHub Pages）
4. **不要**勾选 "Add a README"、".gitignore" 或 "license"（已有）
5. 点击 **Create repository**

### 第2步：推送代码

打开终端（Git Bash / PowerShell），执行：

```bash
cd "C:/Users/33835/Desktop/西班牙语SIELE/siele-workbench-deploy"

# 替换 YOUR_USERNAME 为你的 GitHub 用户名
git remote add origin https://github.com/YOUR_USERNAME/siele-workbench.git
git push -u origin main
```

如果提示输入账号密码：
- Username: 你的 GitHub 用户名
- Password: 你的 GitHub Personal Access Token (不是登录密码)
  - 创建 Token: https://github.com/settings/tokens → Generate new token (classic)
  - 勾选 `repo` 权限即可

### 第3步：启用 GitHub Pages

1. 打开你的仓库页面: `https://github.com/YOUR_USERNAME/siele-workbench`
2. 点击 **Settings** → 左侧 **Pages**
3. Source 选择 **GitHub Actions**
4. 保存

### 第4步：等待部署完成

1. 点击仓库顶部 **Actions** 标签
2. 看到 "Deploy to GitHub Pages" workflow 正在运行
3. 绿色 ✓ 出现后（约1-2分钟），你的站点就上线了！

### 第5步：访问你的永久域名

```
https://YOUR_USERNAME.github.io/siele-workbench/
```

---

## 后续更新

每次修改 `index.html` 后，只需：

```bash
cd "C:/Users/33835/Desktop/西班牙语SIELE/siele-workbench-deploy"
git add -A
git commit -m "update: 更新说明"
git push
```

GitHub Actions 会自动重新部署，1-2分钟后更新生效。

---

## 常见问题

**Q: 推送时提示 "Authentication failed"?**
A: GitHub 已不支持密码认证，必须用 Personal Access Token。去 https://github.com/settings/tokens 创建。

**Q: Pages 显示空白?**
A: 检查 Settings → Pages → Source 是否设为 "GitHub Actions"。

**Q: 想用自定义域名?**
A: 在 Settings → Pages → Custom domain 中添加你的域名，并在域名 DNS 中添加 CNAME 记录指向 `YOUR_USERNAME.github.io`。
